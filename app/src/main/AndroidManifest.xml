<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.masakkuy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cereals"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cereals"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bahansayur_resep"></activity>
        <activity android:name=".bahanlauk_resep" />
        <activity android:name=".bahansayur_suggestion" />
        <activity android:name=".bahanlauk_suggestion" />
        <activity android:name=".bahansayur" />
        <activity android:name=".bahanlauk" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>